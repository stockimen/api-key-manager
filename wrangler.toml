name = "api-key-manager"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

[vars]
# Set ENCRYPTION_KEY via `npx wrangler secret put ENCRYPTION_KEY`

[[kv_namespaces]]
binding = "KV"
# 开发环境使用 preview_id，生产环境部署后需要创建正式 namespace 并填入 id
id = "production_namespace_id_placeholder"
preview_id = "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"  # 替换为实际 preview_id
